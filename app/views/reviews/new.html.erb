<h1>Ajouter une review à : <%= @restaurant.name %> </h1>

<div class="container">
  <div class="col-6 offset-3">
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: Review::NOTES %>
  <%= f.submit 'Ajouter', class: 'btn btn-primary btn-block'%>
<% end %>
  </div>
</div>
